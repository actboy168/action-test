name: CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: macos-11
    steps:
      - uses: actions/checkout@v2
      - uses: actboy168/setup-lua@master
        with:
          luaVersion: 5.4
      - run: lua test.lua
