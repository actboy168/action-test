name: CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  build:
    runs-on: [ubuntu-latest]
    steps:
      - uses: actions/checkout@v3
        with:
          repository: actboy168/luamake
          submodules : recursive
      - name: Build
        uses: uraimo/run-on-arch-action@v2
        with:
          arch: aarch64
          distro: ubuntu20.04
          dockerRunArgs: |
            --volume "${PWD}:/luamake"
          install: |
            apt-get update -q -y
            apt-get install -q -y make gcc g++ ninja-build
          run: |
            set -x
            cd /luamake
            ./compile/build.sh

